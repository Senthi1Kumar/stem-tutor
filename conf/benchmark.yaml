defaults:
  - base_llm: llama

test_cases:
  - prompt: "Explain quantum physics basics"
    max_new_tokens: 256
    temperature: 0.7
  - prompt: "Describe the process of photosynthesis"
    max_new_tokens: 512
    temperature: 0.5

hardware:
  warmup_runs: 3
  measurement_runs: 10
  max_batch_size: 4